# Production reverse proxy — terminates TLS and routes /v1/<svc>/ to each pod.
# Skeleton only; upstreams populated at deploy time.
events {}
http {
    server {
        listen 80;
        # location /v1/auth/ { proxy_pass http://auth-service:8001/; }
        # ...
    }
}
